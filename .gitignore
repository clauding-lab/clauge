node_modules/
.env
.DS_Store
*.log
.cache/
test-data/

# Chrome Web Store build artifacts
clauge-sync-*.zip
clauge-sync-*/

# V3 build outputs
/dist/
src-tauri/target/
src-tauri/gen/
src-tauri/binaries/
sea-prep.blob
# popover/* is the source of truth; build-sidecar.mjs copies into public/popover/
# at build time so SEA's wildcard static route serves the native NSPopover content.
public/popover/
